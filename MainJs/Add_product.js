$(document).ready(function (){    var brabch = '';    $('#choice_branch').change(function (event){        brabch = event.target.value;    })    $('#form_adding_product').submit(function (rvrnt){        event.preventDefault();        var DataForm = {            product_name: $('#ProductName').val(),            product_color: $('#pick_color').val(),            unit_price: $('#unit_price').val(),            stack: $('#stack').val(),            branch: brabch        }        $.ajax({            url: '../Models/Add_Products.php',            type: 'POST',            data: DataForm,            dataType: 'json',            encode: true,            success: function (response){                // check if we have an error                if (response.errors){                    if (response.errors.color){                        $('#color_msg').html(response.errors.color);                        $('#pick_color').addClass('is-invalid')                    }else {                        $('#pick_color').removeClass('is-invalid')                    }                     if (response.errors.productName){                        $('#product_msg').html(response.errors.productName);                        $('#ProductName').addClass('is-invalid')                    }else {                        $('#ProductName').removeClass('is-invalid')                    }                     if (response.errors.stack){                        $('#stack_msg').html(response.errors.stack);                        $('#stack').addClass('is-invalid')                    }else {                        $('#stack').removeClass('is-invalid')                    }                     if (response.errors.price){                        $('#unit_price_msg').html(response.errors.price);                        $('#unit_price').addClass('is-invalid')                    }else {                        $('#unit_price').removeClass('is-invalid')                    }                     if (response.errors.branch){                        $('#branch_msg').html(response.errors.branch);                        $('#choice_branch').addClass('is-invalid')                    }else {                        $('#choice_branch').removeClass('is-invalid')                    }                }                if (response.message === 'New product is successfully created'){                    Swal.fire({                        title: response.message,                        icon: "success",                        confirmButtonColor: '#04aa6d'                    });                    setTimeout(() =>{window.location.reload()},2000);                }            }        })    })})//  UPDATING THE PRODUCTS$(document).ready(function (){})